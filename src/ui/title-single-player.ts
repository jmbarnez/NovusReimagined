import { showProfileSelection } from "./boot-screen/boot-screen-profiles.js";

/** Open the profile selection screen on the boot monitor. */
export function showSinglePlayerMenu(): void {
  showProfileSelection();
}
